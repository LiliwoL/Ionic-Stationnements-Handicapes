import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-larochelle-handicap',
  webDir: 'docs',
  bundledWebRuntime: false
};

export default config;
